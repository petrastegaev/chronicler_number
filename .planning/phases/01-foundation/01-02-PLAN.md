---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/eslint.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/pages/JoinPage.tsx
  - frontend/src/pages/AdminPage.tsx
  - frontend/src/stores/gameStore.ts
  - frontend/src/types/ws.ts
  - frontend/src/vite-env.d.ts
  - frontend/public/fonts/Inter-Regular.woff2
  - frontend/public/fonts/Inter-SemiBold.woff2
  - frontend/public/fonts/Inter-Bold.woff2
  - Dockerfile
  - compose.yml
  - .dockerignore
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-04
must_haves:
  truths:
    - "Frontend builds successfully with npm run build producing frontend/dist/"
    - "React app renders at / (JoinPage placeholder) and /admin (AdminPage placeholder) per D-03"
    - "Tailwind CSS v4 @theme includes all design tokens from UI-SPEC.md color palette"
    - "Inter Regular, SemiBold, and Bold WOFF2 fonts are bundled in frontend/public/fonts/"
    - "Docker multi-stage build completes with frontend/dist/ copied to backend stage"
    - "Docker Compose single-service definition maps port 8000 and volume-mounts data/ per DEPLOY-01"
  artifacts:
    - path: "frontend/package.json"
      provides: "Npm dependency manifest with all required packages"
      contains: "react, react-dom, react-router, zustand, motion, howler, tailwindcss, vite"
    - path: "frontend/src/main.tsx"
      provides: "React entry point with createBrowserRouter setup"
      contains: "RouterProvider, / route, /admin route"
    - path: "frontend/src/index.css"
      provides: "Tailwind CSS v4 with @theme design tokens and @font-face declarations"
      contains: "@import tailwindcss, @theme, @font-face, --color-wb-bg, --font-sans"
    - path: "Dockerfile"
      provides: "Multi-stage build (node:22-alpine + python:3.13-slim)"
      contains: "FROM node:22-alpine AS frontend-build, FROM python:3.13-slim"
    - path: "compose.yml"
      provides: "Single-service Docker Compose definition"
      contains: "ports: 8000, volumes: ./data"
  key_links:
    - from: "Dockerfile"
      to: "frontend/dist/"
      via: "COPY --from=frontend-build"
      pattern: "COPY --from=frontend-build /app/dist ./static"
    - from: "compose.yml"
      to: "Dockerfile"
      via: "build: ."
      pattern: "build: ."
    - from: "frontend/vite.config.ts"
      to: "frontend/src/index.css"
      via: "@tailwindcss/vite plugin processes CSS @import"
      pattern: "tailwindcss()"
---

<objective>
Create the complete frontend scaffold (Vite + React 19 + TypeScript + Tailwind CSS v4 + React Router v7) with all dependencies installed, Inter fonts bundled, and the Docker multi-stage build configuration.

Purpose: Per D-11, D-12, D-13, the frontend scaffold is a full Vite project (not a placeholder). All dependencies are installed now so the Docker build works end-to-end. The Docker multi-stage build (Dockerfile + compose.yml) establishes the single-command deployment per DEPLOY-01. Placeholder pages at / and /admin follow the UI-SPEC.md design contract exactly.
Output: Working frontend/ directory with buildable Vite project, Inter fonts in public/fonts/, Dockerfile, compose.yml, and .dockerignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

The UI-SPEC.md contains the exact CSS and TSX contents for:
- index.css (complete @theme, @font-face, @layer base)
- JoinPage.tsx (placeholder with game title, status, badge)
- AdminPage.tsx (placeholder with game title, admin heading, badge)
- App.tsx (Outlet layout)
- main.tsx (createBrowserRouter with / and /admin)

RESEARCH.md Pattern 4 and Pattern 5 contain the vite.config.ts and main.tsx patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend project with config files, source files, and install dependencies</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/vite.config.ts
    frontend/eslint.config.js
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/index.css
    frontend/src/pages/JoinPage.tsx
    frontend/src/pages/AdminPage.tsx
    frontend/src/stores/gameStore.ts
    frontend/src/types/ws.ts
    frontend/src/vite-env.d.ts
  </files>
  <read_first>
    UI-SPEC.md sections "Concrete Deliverables for Phase 1" items 1-5 and 7 for exact file contents
    PATTERNS.md sections "frontend/vite.config.ts", "frontend/src/index.css", "frontend/src/main.tsx", "frontend/src/App.tsx", "frontend/src/pages/JoinPage.tsx", "frontend/src/pages/AdminPage.tsx", "frontend/src/stores/gameStore.ts", "frontend/src/types/ws.ts"
    RESEARCH.md Pattern 4 (Tailwind CSS v4 with Vite Plugin) for vite.config.ts
    RESEARCH.md Pattern 5 (React Router v7 with createBrowserRouter) for main.tsx
  </read_first>
  <action>
    Create the frontend/ directory and all files. This is a greenfield project -- no Vite template scaffolding command is used; all files are created manually for full control.

    **Step 1: Create directories**
    ```bash
    mkdir -p frontend/src/pages frontend/src/stores frontend/src/types frontend/public/fonts
    ```

    **Step 2: Create `frontend/package.json`**
    ```json
    {
      "name": "number-duel-frontend",
      "private": true,
      "version": "1.0.0",
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "preview": "vite preview",
        "lint": "eslint ."
      },
      "dependencies": {
        "react": "^19.0.0",
        "react-dom": "^19.0.0",
        "react-router": "^7.0.0",
        "zustand": "^5.0.0",
        "motion": "^12.0.0",
        "howler": "^2.2.4"
      },
      "devDependencies": {
        "@vitejs/plugin-react": "^4.0.0",
        "typescript": "^5.0.0",
        "@types/react": "^19.0.0",
        "@types/react-dom": "^19.0.0",
        "vite": "^6.0.0",
        "tailwindcss": "^4.3.0",
        "@tailwindcss/vite": "^4.3.0",
        "eslint": "^9.0.0",
        "prettier": "^3.0.0"
      }
    }
    ```

    **Step 3: Create `frontend/tsconfig.json`**
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "useDefineForClassFields": true,
        "lib": ["ES2020", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "skipLibCheck": true,
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "isolatedModules": true,
        "moduleDetection": "force",
        "noEmit": true,
        "jsx": "react-jsx",
        "strict": true,
        "noUnusedLocals": false,
        "noUnusedParameters": false,
        "noFallthroughCasesInSwitch": true,
        "forceConsistentCasingInFileNames": true
      },
      "include": ["src"]
    }
    ```

    **Step 4: Create `frontend/vite.config.ts`**
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
    })
    ```

    **Step 5: Create `frontend/eslint.config.js`**
    ```javascript
    import js from '@eslint/js'
    import tseslint from 'typescript-eslint'

    export default tseslint.config(
      { ignores: ['dist'] },
      {
        extends: [js.configs.recommended, ...tseslint.configs.recommended],
        files: ['**/*.{ts,tsx}'],
      }
    )
    ```
    Note: Since we are not installing `@eslint/js` or `typescript-eslint` as separate packages, this config serves as a declarative scaffold. The build does not depend on it. The executor should create this file but not worry about import resolution for dev-only eslint plugins.

    **Step 6: Create `frontend/index.html`**
    ```html
    <!DOCTYPE html>
    <html lang="ru">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Дуэль чисел</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    **Step 7: Create `frontend/src/vite-env.d.ts`**
    ```typescript
    /// <reference types="vite/client" />
    ```

    **Step 8: Create `frontend/src/index.css`** -- exact copy from UI-SPEC.md section "Concrete Deliverables > 1. frontend/src/index.css":
    ```css
    @import "tailwindcss";

    @font-face {
      font-family: 'Inter';
      src: url('/fonts/Inter-Regular.woff2') format('woff2');
      font-weight: 400;
      font-style: normal;
    }

    @font-face {
      font-family: 'Inter';
      src: url('/fonts/Inter-SemiBold.woff2') format('woff2');
      font-weight: 600;
      font-style: normal;
    }

    @font-face {
      font-family: 'Inter';
      src: url('/fonts/Inter-Bold.woff2') format('woff2');
      font-weight: 700;
      font-style: normal;
    }

    @theme {
      --color-wb-bg: #1a0a2e;
      --color-wb-surface: #2d1b4e;
      --color-wb-text: #eeeeee;
      --color-wb-text-muted: #9ca3af;
      --color-player1: #3B82F6;
      --color-player1-light: #60a5fa;
      --color-player2: #EF4444;
      --color-player2-light: #f87171;
      --color-correct: #10B981;
      --color-warning: #F59E0B;
      --color-danger: #EF4444;
      --font-sans: 'Inter', system-ui, sans-serif;
      --font-mono: 'JetBrains Mono', 'Fira Code', monospace;
    }

    @layer base {
      body {
        background-color: var(--color-wb-bg);
        color: var(--color-wb-text);
        font-family: var(--font-sans);
        font-size: 16px;
        line-height: 1.5;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }
    }
    ```

    **Step 9: Create `frontend/src/main.tsx`** -- exact from UI-SPEC.md and RESEARCH.md Pattern 5:
    ```typescript
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import { createBrowserRouter, RouterProvider } from 'react-router'
    import App from './App'
    import JoinPage from './pages/JoinPage'
    import AdminPage from './pages/AdminPage'
    import './index.css'

    const router = createBrowserRouter([
      {
        path: '/',
        Component: App,
        children: [
          { index: true, Component: JoinPage },
          { path: 'admin', Component: AdminPage },
        ],
      },
    ])

    const root = document.getElementById('root')
    if (root) {
      root.innerHTML = ''
      createRoot(root).render(
        <StrictMode>
          <RouterProvider router={router} />
        </StrictMode>
      )
    }
    ```

    **Step 10: Create `frontend/src/App.tsx`**
    ```typescript
    import { Outlet } from 'react-router'

    export default function App() {
      return <Outlet />
    }
    ```

    **Step 11: Create `frontend/src/pages/JoinPage.tsx`** -- exact from UI-SPEC.md:
    ```typescript
    export default function JoinPage() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center bg-wb-bg px-4">
          <h1 className="text-4xl font-bold text-wb-text">Дуэль чисел</h1>
          <p className="mt-2 text-2xl font-semibold text-player1">Экран входа</p>
          <p className="mt-4 text-base text-wb-text-muted">
            Подключение первого игрока...
          </p>
          <span className="mt-8 rounded bg-wb-surface px-3 py-1 text-sm text-wb-text-muted">
            Заглушка
          </span>
        </main>
      )
    }
    ```

    **Step 12: Create `frontend/src/pages/AdminPage.tsx`** -- exact from UI-SPEC.md:
    ```typescript
    export default function AdminPage() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center bg-wb-bg px-4">
          <h1 className="text-4xl font-bold text-wb-text">Дуэль чисел</h1>
          <p className="mt-2 text-2xl font-semibold text-player2">
            Панель ведущего
          </p>
          <p className="mt-4 text-base text-wb-text-muted">
            Управление игрой
          </p>
          <span className="mt-8 rounded bg-wb-surface px-3 py-1 text-sm text-wb-text-muted">
            Заглушка
          </span>
        </main>
      )
    }
    ```

    **Step 13: Create `frontend/src/stores/gameStore.ts`** -- Zustand store shell:
    ```typescript
    import { create } from 'zustand'

    interface GameState {
      phase: string
    }

    export const useGameStore = create<GameState>((set) => ({
      phase: 'idle',
    }))
    ```

    **Step 14: Create `frontend/src/types/ws.ts`** -- WebSocket message types:
    ```typescript
    export interface WsMessage {
      event: string
      data: Record<string, unknown>
    }

    export interface JoinMessage extends WsMessage {
      event: 'join'
      data: {
        role: 'player' | 'admin'
        nickname: string
      }
    }

    export interface JoinedEvent extends WsMessage {
      event: 'joined'
      data: {
        role: 'player' | 'admin'
        player_number?: number
        nickname?: string
      }
    }

    export interface ErrorEvent extends WsMessage {
      event: 'error'
      data: {
        message: string
      }
    }
    ```

    **Step 15: Install npm dependencies**
    ```bash
    cd /home/petr/IdeaProjects/PetProjects/number_game/frontend && npm install
    ```

    **Step 16: Verify the TypeScript build works**
    ```bash
    cd /home/petr/IdeaProjects/PetProjects/number_game/frontend && npx tsc --noEmit
    ```
    If tsc fails due to missing type declarations for howler or motion, add `"skipLibCheck": true` (already in tsconfig.json). If it fails due to eslint config import resolution, ignore eslint config errors -- the build uses tsc + vite, not eslint during build.
  </action>
  <verify>
    <automated>cd /home/petr/IdeaProjects/PetProjects/number_game/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. `npm run build` in frontend/ exits 0 and produces frontend/dist/index.html
    2. frontend/dist/index.html contains "Дуэль чисел" in the title
    3. frontend/src/index.css contains all 10 color tokens from UI-SPEC palette
    4. frontend/src/main.tsx contains createBrowserRouter with / and /admin routes
    5. frontend/package.json contains all 6 dependencies (react, react-dom, react-router, zustand, motion, howler)
  </acceptance_criteria>
  <done>Frontend builds successfully with all placeholder pages rendering correct Russian copy. All dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Download Inter fonts and create Docker multi-stage build configuration</name>
  <files>
    frontend/public/fonts/Inter-Regular.woff2
    frontend/public/fonts/Inter-SemiBold.woff2
    frontend/public/fonts/Inter-Bold.woff2
    Dockerfile
    compose.yml
    .dockerignore
  </files>
  <read_first>
    RESEARCH.md section "Code Examples > Docker Multi-stage Build" for Dockerfile and compose.yml exact contents
    UI-SPEC.md section "Font" for Inter font bundling requirement
  </read_first>
  <action>
    **Step 1: Download Inter WOFF2 font files**
    ```bash
    mkdir -p /home/petr/IdeaProjects/PetProjects/number_game/frontend/public/fonts
    curl -sSL -o /home/petr/IdeaProjects/PetProjects/number_game/frontend/public/fonts/Inter-Regular.woff2 \
      "https://github.com/rsms/inter/raw/master/docs/font-files/Inter-Regular.woff2"
    curl -sSL -o /home/petr/IdeaProjects/PetProjects/number_game/frontend/public/fonts/Inter-SemiBold.woff2 \
      "https://github.com/rsms/inter/raw/master/docs/font-files/Inter-SemiBold.woff2"
    curl -sSL -o /home/petr/IdeaProjects/PetProjects/number_game/frontend/public/fonts/Inter-Bold.woff2 \
      "https://github.com/rsms/inter/raw/master/docs/font-files/Inter-Bold.woff2"
    ```
    Verify each file downloaded successfully (all should be non-empty WOFF2 files).

    **Step 2: Create `Dockerfile`**
    ```dockerfile
    # Stage 1: Build React frontend
    FROM node:22-alpine AS frontend-build
    WORKDIR /app
    COPY frontend/package*.json ./
    RUN npm ci
    COPY frontend/ ./
    RUN npm run build

    # Stage 2: Python backend
    FROM python:3.13-slim
    WORKDIR /app
    COPY backend/requirements.txt .
    RUN pip install --no-cache-dir -r requirements.txt
    COPY backend/ ./

    # Copy built frontend from Stage 1
    COPY --from=frontend-build /app/dist ./static

    # Create data directory for SQLite
    RUN mkdir -p /app/data

    # Non-root user for security
    RUN adduser --disabled-password appuser && chown -R appuser /app
    USER appuser

    EXPOSE 8000
    CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
    ```

    **Step 3: Create `compose.yml`**
    ```yaml
    services:
      app:
        build: .
        ports:
          - "8000:8000"
        volumes:
          - ./data:/app/data
    ```

    **Step 4: Create `.dockerignore`**
    ```
    __pycache__/
    .git/
    .env
    .venv/
    *.pyc
    node_modules/
    frontend/node_modules/
    .planning/
    data/
    ```

    **Step 5: Verify Docker build works**
    ```bash
    cd /home/petr/IdeaProjects/PetProjects/number_game && docker compose build 2>&1 | tail -10
    ```
    If the build fails (e.g., aiosqlite not installable, Python 3.13-slim image issues), report the error but do not block -- the full end-to-end verification happens in Plan 01-03 when all backend files exist. The Docker build can only succeed after Plan 01-01 creates backend/requirements.txt, which is already in Wave 1 parallel.
  </action>
  <verify>
    <automated>test -f frontend/public/fonts/Inter-Regular.woff2 && test -f frontend/public/fonts/Inter-SemiBold.woff2 && test -f frontend/public/fonts/Inter-Bold.woff2 && echo "FONTS DOWNLOADED" && test -f Dockerfile && test -f compose.yml && test -f .dockerignore && echo "DOCKER FILES CREATED"</automated>
  </verify>
  <acceptance_criteria>
    1. frontend/public/fonts/Inter-Regular.woff2 exists and is non-empty (>10KB)
    2. frontend/public/fonts/Inter-SemiBold.woff2 exists and is non-empty
    3. frontend/public/fonts/Inter-Bold.woff2 exists and is non-empty
    4. Dockerfile contains both FROM lines (node:22-alpine and python:3.13-slim)
    5. Dockerfile contains non-root USER appuser
    6. compose.yml contains ports: "8000:8000" and volumes mapping ./data:/app/data
  </acceptance_criteria>
  <done>Inter fonts bundled locally. Docker multi-stage build and Compose configuration created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host -> container | The container runs with a non-root user. SQLite data volume is mounted from host. No port mapping to privileged ports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | Dockerfile | mitigate | Dockerfile creates non-root user `appuser` and runs CMD as that user. Files are chown'd to appuser. No privileged port mapping. |
| T-02-02 | Information Disclosure | compose.yml volume mount | accept | SQLite database is stored on the host at ./data. This is a local LAN conference booth -- no sensitive data, no multi-tenant isolation needed. |
| T-02-03 | Tampering | frontend source files | accept | Placeholder pages have no user inputs, no forms, no API calls. No attack surface in Phase 1 frontend. Actual input validation begins in Plans 01-03 (REST API) and Phase 3 (player input). |
</threat_model>

<verification>
1. `npm run build` succeeds in frontend/ -- confirms TypeScript compiles, Vite bundles, Tailwind processes @theme
2. `docker compose build` completes -- confirms Docker multi-stage build works
3. Frontend placeholder pages are buildable -- Theme tokens, Inter fonts, and React Router structure are all correct
</verification>

<success_criteria>
1. Frontend builds to frontend/dist/ with no errors
2. Inter fonts (Regular 400, SemiBold 600, Bold 700) are present in frontend/public/fonts/ as WOFF2 files
3. Dockerfile produces a buildable image (multi-stage)
4. compose.yml defines the single-service app with port 8000 and data volume
5. All 6 frontend runtime dependencies (react, react-dom, react-router, zustand, motion, howler) are installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
