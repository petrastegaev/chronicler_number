---
phase: 05-audio-deployment-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/Dockerfile
  - compose.yml
autonomous: true
requirements:
  - AUDIO-06
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "All 4 audio files are bundled in the Docker image — no external CDN requests verified via DevTools network tab"
    - "`docker compose up --build` starts the complete application without errors"
    - "Application works fully offline with no internet connectivity"
    - "Audio files are served at /sounds/{name}.mp3 with HTTP 200"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build that includes audio files in the final image"
      pattern: "COPY frontend/"
    - path: "compose.yml"
      provides: "Docker Compose configuration (no changes needed — verify only)"
    - path: "frontend/Dockerfile.verify"
      provides: "Verification script for Docker image audio file presence"
  key_links:
    - from: "Dockerfile (frontend-build stage)"
      to: "frontend/public/sounds/"
      via: "COPY frontend/ ./"
      pattern: "COPY frontend/ ./"
    - from: "Dockerfile (python stage)"
      to: "frontend-build:/app/dist"
      via: "COPY --from=frontend-build /app/dist ./static"
      pattern: "./static"
---

<objective>
Verify and harden deployment: confirm Docker image includes audio files, execute full offline verification checklist, document verification results.

Purpose: Ensure the complete application (with sounds) deploys correctly via Docker Compose and works fully offline — the critical requirement for the conference booth. No code changes are expected to Dockerfile or compose.yml (audio files are already included via existing COPY patterns). This plan produces a verification document and runs the verification procedure.
Output: Verification results confirming all 4 audio files in Docker image, offline operation verified, zero external CDN requests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Docker deployment configs
@Dockerfile
@compose.yml

# Phase 5 research and patterns
@.planning/phases/05-audio-deployment-polish/05-RESEARCH.md
@.planning/phases/05-audio-deployment-polish/05-PATTERNS.md

# Phase 5 audio implementation (from Plan 01)
@.planning/phases/05-audio-deployment-polish/05-01-SUMMARY.md

# Existing verification patterns
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Docker image includes audio files and build succeeds</name>
  <files>
    Dockerfile
    compose.yml
  </files>
  <read_first>
    @Dockerfile
    @compose.yml
  </read_first>
  <action>
    **Read before modifying:** Read `Dockerfile` and `compose.yml` fully to confirm their current state.

    **Step 1 — Confirm Dockerfile needs no changes:**
    - Line 6: `COPY frontend/ ./` copies the entire `frontend/` directory including `public/sounds/` into the build context
    - Line 7: `RUN npm run build` — Vite copies `public/` to `dist/`, so `dist/sounds/` is created
    - Line 17: `COPY --from=frontend-build /app/dist ./static` — `dist/sounds/` becomes `static/sounds/`
    - FastAPI StaticFiles mount at `/` serves them at `/sounds/{name}.mp3`
    - **Result: No Dockerfile changes needed.**

    **Step 2 — Confirm compose.yml needs no changes:**
    - Line 7: `volumes: - ./data:/app/data` is for SQLite persistence only
    - Audio files are embedded in the image via Dockerfile COPY
    - **Result: No compose.yml changes needed.**

    **Step 3 — Run build and verify:**
    ```bash
    # Full rebuild to ensure no stale cache
    docker compose build --no-cache

    # Check audio files in the built image
    # First get the image name from docker compose
    IMAGE_NAME=$(docker compose config --images 2>/dev/null | head -1 || echo "number_game-app")
    docker run --rm "$IMAGE_NAME" ls -la /app/static/sounds/ 2>/dev/null || \
      docker run --rm "$(docker images --format '{{.Repository}}:{{.Tag}}' | head -1)" ls -la /app/static/sounds/

    # Verify MP3 headers
    curl -s http://localhost:8000/sounds/tick.mp3 | head -c 20
    ```

    If `docker compose config --images` does not work, use `docker compose build` then inspect images.
  </action>
  <verify>
    <automated>
      # Verify audio files exist in Docker image
      IMG=$(docker compose images -q app 2>/dev/null)
      if [ -z "$IMG" ]; then
        echo "Image not yet built — building..."
        docker compose build
        IMG=$(docker compose images -q app)
      fi
      docker run --rm "$IMG" ls -la /app/static/sounds/ | grep -q "tick.mp3" || exit 1
      docker run --rm "$IMG" ls -la /app/static/sounds/ | grep -q "tick_fast.mp3" || exit 1
      docker run --rm "$IMG" ls -la /app/static/sounds/ | grep -q "end_round.mp3" || exit 1
      docker run --rm "$IMG" ls -la /app/static/sounds/ | grep -q "winner.mp3" || exit 1
      echo "All 4 audio files verified in Docker image at /app/static/sounds/"
    </automated>
    <human-check>
      Verify http://localhost:8000/sounds/tick.mp3 returns MP3 data (not HTML) in browser
    </human-check>
  </verify>
  <done>Docker image built successfully; ls -la confirms 4 MP3 files in /app/static/sounds/; curl returns MP3 bytes for each</done>
  <acceptance_criteria>
    - `docker compose build --no-cache` exits with code 0
    - `docker run --rm $IMAGE ls -la /app/static/sounds/` shows tick.mp3, tick_fast.mp3, end_round.mp3, winner.mp3 all present
    - `curl -s http://localhost:8000/sounds/tick.mp3 | head -c 20` returns MP3 header bytes (starts with `ID3` or `\xff\xfb`), not HTML
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Execute offline verification checklist</name>
  <files>
    .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
  </files>
  <read_first>
    @.planning/phases/05-audio-deployment-polish/05-RESEARCH.md (section "Offline Verification Checklist")
    @.planning/phases/01-foundation/01-VERIFICATION.md (existing verification format)
  </read_first>
  <action>
    Create `.planning/phases/05-audio-deployment-polish/05-VERIFICATION.md` documenting the offline verification procedure and results.

    The verification document should follow the existing format from `01-VERIFICATION.md` and include:

    1. **Prerequisites**: Docker, Docker Compose, browser (preferably Chrome DevTools), 3 browser tabs open (2 player + 1 admin)

    2. **Test 1: Docker build** -- `docker compose build --no-cache` succeeds

    3. **Test 2: Offline startup** -- Disconnect machine from all networks (WiFi off, ethernet unplugged), run `docker compose up`, verify app starts within 30 seconds

    4. **Test 3: Static file serving** -- Verify `http://localhost:8000/sounds/tick.mp3` returns MP3 bytes for all 4 files. Check HTTP status 200.

    5. **Test 4: No external CDN requests** -- Open DevTools > Network tab, filter by domain (not localhost), verify zero external requests during page load and game play

    6. **Test 5: Audio playback during game** -- Play a full game (join 2 players, admin starts, complete 9 rounds), verify:
       - Tick sound plays each second during countdown
       - Tick_fast activates at <= 3 seconds remaining
       - End_round bell plays after each round result
       - Winner fanfare plays at game end

    7. **Test 6: AudioContext unlock** -- On mobile or desktop with autoplay blocking, verify join button click unlocks audio (sounds play after clicking "Присоединиться")

    8. **Test 7: Graceful degradation** -- Block audio files in DevTools or test without speakers, verify game continues without errors

    9. **Captive portal information** -- Add informational section for booth staff:
       - If venue WiFi shows a login page, select "Use network without internet" / "Skip"
       - No app-level mitigation possible for OS captive portal detection
       - Recommended: bring a dedicated portable router (GL.iNet travel router) as fallback
       - The app runs on plain HTTP (`http://<server-ip>:8000/`) which typically bypasses captive portal detection

    Format each test as:

    ```
    ### Test {N}: {Test name}
    **Steps:**
    1. ...
    2. ...
    **Expected:**
    **Result:** [PASS/FAIL]
    ```

    Leave **Result:** as `[PASS/FAIL]` (filled by human during verification). Mark the file as a template with instructions to fill in.

    Include at the top:
    ```md
    # Phase 5: Audio + Deployment Polish — VERIFICATION
    **Date:** 2026-06-11
    **Environment:** Docker, Docker Compose, offline LAN
    **Status:** INCOMPLETE (fill results below)
    ```
  </action>
  <verify>
    <automated>MISSING — file-based documentation verification; use grep to check structure:
      grep -q "Test 1" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
      grep -q "Test 2" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
      grep -q "Test 3" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
      grep -q "Test 4" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
      grep -q "Test 5" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
      grep -q "Test 6" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
      grep -q "Test 7" .planning/phases/05-audio-deployment-polish/05-VERIFICATION.md
    </automated>
  </verify>
  <done>5-VERIFICATION.md created with all 7 test procedures documented; Docker image verification from Task 1 results included</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build (host -> image) | Build process controls what files enter the container image |
| Static file serving (FastAPI -> browser) | Audio files served via existing StaticFiles mount |
| Network boundary (host <-> internet) | Offline verification ensures zero external requests |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | Docker build cache | accept | Build cache may skip COPY if `public/sounds/` is new; mitigated by `docker compose build --no-cache` before event day |
| T-05-06 | Information Disclosure | Audio files served to unauthenticated clients | accept | All static assets are public by design (SPA); no sensitive data in audio files |
| T-05-07 | Denial of Service | External CDN dependency failure | mitigate | AUDIO-06 requires zero external CDN; offline verification test explicitly checks for zero external requests |
| T-05-08 | Spoofing | Captive portal redirect | accept | OS-level behavior outside app control; documented as informational booth staff guidance |
</threat_model>

<verification>
1. `docker compose build --no-cache` exits 0
2. Audio files present at /app/static/sounds/ in the Docker image
3. curl to http://localhost:8000/sounds/{name}.mp3 returns 200 + MP3 bytes for all 4 files
4. 5-VERIFICATION.md documents all 7 tests with clear pass/fail criteria
5. No changes needed to Dockerfile or compose.yml (verified by read)
</verification>

<success_criteria>
- Docker image includes all 4 audio files at /app/static/sounds/
- Offline verification checklist created and fillable by human
- Zero Dockerfile or compose.yml changes required (audio files included via existing build patterns)
- Booth staff have clear captive portal documentation for venue WiFi
</success_criteria>

<output>
Create `.planning/phases/05-audio-deployment-polish/05-02-SUMMARY.md` when done
</output>
