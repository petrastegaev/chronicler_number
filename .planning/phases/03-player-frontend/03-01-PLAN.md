---
phase: 03-player-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/stores/gameStore.ts
  - frontend/src/pages/JoinPage.tsx
files_created:
  - frontend/src/components/GameScreen.tsx
  - frontend/src/components/JoinScreen.tsx
  - frontend/src/components/WaitingScreen.tsx
  - frontend/src/components/PlayingScreen.tsx
  - frontend/src/components/TimerRing.tsx
  - frontend/src/components/AnswerInput.tsx
  - frontend/src/components/GameHeader.tsx
  - frontend/src/hooks/useWebSocket.ts
autonomous: true
requirements:
  - JOIN-01
  - JOIN-03
  - JOIN-04
  - GAME-01
  - GAME-03
user_setup: []

must_haves:
  truths:
    - "Player navigates to / and sees a nickname input with 'Присоединиться' button (JOIN-01)"
    - "After submitting nickname, player sees 'Ожидание соперника...' while alone, then 'Ожидание запуска администратором' when both ready (JOIN-03, JOIN-04)"
    - "When admin starts the game, both players see 'Раунд 1 / 9' with question text and a circular timer showing 10 seconds (GAME-01)"
    - "Player can type an integer answer in a numeric input; submit button sends answer and shows 'Ответ принят'; auto-submit fires on timer expiry (GAME-03)"
    - "WebSocket connects to server and routes all game events to Zustand store actions"
    - "GameHeader shows game title, player nickname, score, and round indicator during playing phase"
  artifacts:
    - path: "frontend/src/stores/gameStore.ts"
      provides: "Extended Zustand store with ws, submittedAnswer, myAnswer, roundResult, gameEndResult fields and setSubmittedAnswer/setMyAnswer/resetRound actions"
      exports: ["useGameStore"]
      min_lines: 90
    - path: "frontend/src/hooks/useWebSocket.ts"
      provides: "WebSocket lifecycle hook with connect(), join(nickname), submitAnswer(answer) functions and message routing to store"
      exports: ["useWebSocket"]
      min_lines: 80
    - path: "frontend/src/components/GameScreen.tsx"
      provides: "Root player component at route / with AnimatePresence-driven phase-based rendering of JoinScreen/WaitingScreen/PlayingScreen/ResultOverlay/FinalScreen"
      min_lines: 50
    - path: "frontend/src/components/JoinScreen.tsx"
      provides: "JOIN-01 nickname input (max 15 chars) + 'Присоединиться' button; manages idle/submitting states per UI-SPEC"
      min_lines: 40
    - path: "frontend/src/components/WaitingScreen.tsx"
      provides: "JOIN-03/JOIN-04 waiting states: 'Ожидание соперника...' or 'Ожидание запуска администратором' based on store state"
      min_lines: 30
    - path: "frontend/src/components/PlayingScreen.tsx"
      provides: "GAME-01 active round view: question text, TimerRing, AnswerInput, GameHeader; renders when phase == 'playing'"
      min_lines: 40
    - path: "frontend/src/components/TimerRing.tsx"
      provides: "D-01/D-02/D-03 SVG circular timer (88px, counter-clockwise, pathLength animation, color transitions at <=5s yellow and <=3s red)"
      min_lines: 50
    - path: "frontend/src/components/AnswerInput.tsx"
      provides: "D-08/D-09/D-10/D-11 numeric input (type=number, 0-1M, 36px font, 56px height), 'Ответить' button, early submit, auto-submit guard, 'Ответ принят' confirmation"
      min_lines: 60
    - path: "frontend/src/components/GameHeader.tsx"
      provides: "D-14 persistent header: 'Дуэль чисел' title, player nickname (accent color), score, 'Раунд N / 9' indicator; visible during playing/showing_result/finished"
      min_lines: 35
    - path: "frontend/src/pages/JoinPage.tsx"
      provides: "Replaced placeholder with <GameScreen> component rendering"
      min_lines: 10
  key_links:
    - from: "frontend/src/pages/JoinPage.tsx"
      to: "frontend/src/components/GameScreen.tsx"
      via: "import GameScreen"
      pattern: "import.*GameScreen"
    - from: "frontend/src/components/GameScreen.tsx"
      to: "frontend/src/stores/gameStore.ts"
      via: "useGameStore for phase selection"
      pattern: "useGameStore"
    - from: "frontend/src/hooks/useWebSocket.ts"
      to: "frontend/src/stores/gameStore.ts"
      via: "useGameStore.getState() and .setState() outside React"
      pattern: "useGameStore\\.(getState|setState|setPhase|setPlayerNumber)"
    - from: "frontend/src/hooks/useWebSocket.ts"
      to: "frontend/src/types/ws.ts"
      via: "import type WsMessage"
      pattern: "import.*WsMessage"
    - from: "frontend/src/components/AnswerInput.tsx"
      to: "frontend/src/hooks/useWebSocket.ts"
      via: "useWebSocket hook submitAnswer"
      pattern: "submitAnswer"
    - from: "frontend/src/components/PlayingScreen.tsx"
      to: "frontend/src/components/TimerRing.tsx"
      via: "import TimerRing"
      pattern: "import.*TimerRing"
    - from: "frontend/src/components/PlayingScreen.tsx"
      to: "frontend/src/components/AnswerInput.tsx"
      via: "import AnswerInput"
      pattern: "import.*AnswerInput"
---

<objective>
Build the core player frontend: WebSocket integration, Zustand store extension, and three primary screens (Join, Waiting, Playing) with circular timer and answer input.

**Purpose:** This is the foundation of the player experience -- connection to the game server, nickname entry, waiting lobby, and the active round gameplay with question display, countdown timer, and answer submission.

**Output:** Extended Zustand store, WebSocket hook, GameScreen router, JoinScreen, WaitingScreen, PlayingScreen, TimerRing, AnswerInput, GameHeader, and updated JoinPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-player-frontend/03-CONTEXT.md
@.planning/phases/03-player-frontend/03-RESEARCH.md
@.planning/phases/03-player-frontend/03-UI-SPEC.md
@.planning/phases/02-core-game-loop/02-CONTEXT.md
@.planning/phases/02-core-game-loop/02-01-SUMMARY.md
@.planning/phases/02-core-game-loop/02-02-SUMMARY.md
@frontend/src/types/ws.ts
@frontend/src/stores/gameStore.ts
@frontend/src/index.css
@frontend/src/App.tsx
@frontend/src/main.tsx
@frontend/src/pages/JoinPage.tsx
@frontend/vite.config.ts
@frontend/package.json
@gdd.md
@web_design.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Zustand store and create WebSocket hook</name>

  <files>
    frontend/src/stores/gameStore.ts
    frontend/src/hooks/useWebSocket.ts
  </files>

  <read_first>
    frontend/src/stores/gameStore.ts -- existing store structure, fields, actions, initialState
    frontend/src/types/ws.ts -- all 11 WebSocket event interfaces (RoundResultEvent, GameEndEvent, etc.)
  </read_first>

  <action>
    Extend `frontend/src/stores/gameStore.ts`:

    1. Add these fields to the `GameState` interface (keeping all existing fields):
       - `ws: WebSocket | null` (initial: null) -- store WebSocket reference for non-React access per RESEARCH.md Pattern 1
       - `submittedAnswer: boolean` (initial: false) -- D-09 guard against double-submit
       - `myAnswer: number | null` (initial: null) -- D-11 local echo value
       - `roundResult: object | null` (initial: null) -- full RoundResultEvent['data'] shape
         - Fields: `round_number: number`, `correct_answer: number`, `player1_answer: number | null`, `player2_answer: number | null`, `winner: 'player1' | 'player2' | 'draw'`
       - `gameEndResult: object | null` (initial: null) -- full GameEndEvent['data'] shape
         - Fields: `player1_nickname: string`, `player2_nickname: string`, `player1_score: number`, `player2_score: number`, `winner: string | null`, `rounds: Array<...>`

    2. Add these actions to the `GameActions` interface:
       - `setSubmittedAnswer: (value: boolean) => void` -- marks answer as submitted (D-09)
       - `setMyAnswer: (value: number | null) => void` -- sets local echo value (D-11)
       - `resetRound: () => void` -- resets submittedAnswer=false, myAnswer=null, roundResult=null on each round_started

    3. Update the `create<GameStore>` call:
       - Add the three new action implementations
       - Update `reset: () => set(initialState)` to reference an `initialState` that's an exported const
       - The `initialState` constant (currently inline) must be used by `reset()`

    4. Update `setTimer` action: add `submittedAnswer: false` guard reset -- not strictly needed but prevents stale state.
       Actually, `resetRound` handles this. Keep `setTimer` as-is.

    Create `frontend/src/hooks/useWebSocket.ts`:

    1. Import and pattern follow RESEARCH.md "Pattern 1: WebSocket Message Handler (Non-React Store Access)" exactly.
       - Import `useGameStore` from `../stores/gameStore`
       - Import `WsMessage` type from `../types/ws`
       - Import `useCallback`, `useEffect`, `useRef` from `react`

    2. Export `function useWebSocket()` returning `{ connect, join, submitAnswer }`:
       - `connect`: Creates WebSocket to `ws://host:8000/ws` (protocol derived from window.location). On `onopen`, stores ws in store via `useGameStore.setState({ ws })`.
       - On `onmessage`: Parse JSON as WsMessage, then switch on `msg.event`:
         - `joined`: set `playerNumber` from `data.player_number`, set phase to `'waiting'`
         - `game_started`: call `setGameStarted(data.player1_nickname, data.player2_nickname)`
         - `round_started`: call `setRoundStarted`, also call `resetRound()`
         - `timer_tick`: call `setTimer(data.remaining)`
         - `round_result`: store full data in `roundResult`, set phase to `'showing_result'`
         - `score_update`: call `setScoreUpdate(data.player1_score, data.player2_score)`
         - `game_end`: store full data in `gameEndResult`, set phase to `'finished'`
         - `game_reset`: call `reset()`, set phase to `'waiting'`
         - `state_snapshot`: restore state per RESEARCH.md pattern
       - On `onclose`: set `ws: null`, phase to `'idle'`
       - All message handling uses `useGameStore.getState()` and `useGameStore.setState()` (not selector hooks) per RESEARCH.md Pitfall 1 guidance.
       - `join(nickname)`: set phase to `'joining'`, send `{event: "join", data: {role: "player", nickname}}`
       - `submitAnswer(answer)`: send `{event: "submit_answer", data: {answer}}`
       - Cleanup effect: close WebSocket on unmount

    3. Use `useCallback` for all three returned functions. Use `useRef` for the WebSocket instance.

    **Connection detail:** The RESEARCH.md pattern derives host from `window.location.host` which includes port. No port override needed -- same host as the page. Include credentials for session cookie via third arg `new WebSocket(url)`. The session cookie is set by the server on first HTTP request and is sent automatically by the browser on WebSocket connections to the same origin.

    **No npm install needed** -- all packages already in package.json.
  </action>

  <acceptance_criteria>
    - `frontend/src/stores/gameStore.ts` contains `ws: WebSocket | null` in GameState
    - `frontend/src/stores/gameStore.ts` contains `submittedAnswer: boolean` in GameState
    - `frontend/src/stores/gameStore.ts` contains `myAnswer: number | null` in GameState
    - `frontend/src/stores/gameStore.ts` contains `roundResult` in GameState (object with round_number, correct_answer, player1_answer, player2_answer, winner fields)
    - `frontend/src/stores/gameStore.ts` contains `gameEndResult` in GameState (object with player1_nickname, player2_nickname, player1_score, player2_score, winner, rounds fields)
    - `frontend/src/stores/gameStore.ts` contains `setSubmittedAnswer`, `setMyAnswer`, `resetRound` in GameActions
    - `frontend/src/stores/gameStore.ts` exports `useGameStore`
    - `frontend/src/hooks/useWebSocket.ts` exists and exports `function useWebSocket()`
    - `frontend/src/hooks/useWebSocket.ts` contains `connect`, `join`, `submitAnswer` in return
    - `frontend/src/hooks/useWebSocket.ts` contains `useGameStore.getState()` and `.setState()` in onmessage handler
    - `frontend/src/hooks/useWebSocket.ts` handles all 8 event types: joined, game_started, round_started, timer_tick, round_result, score_update, game_end, game_reset, state_snapshot
    - `npx tsc --noEmit` compiles clean from the frontend/ directory
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1</automated>
  </verify>

  <done>
    Zustand store extended with all Phase 3 fields and actions. WebSocket hook created with connect/join/submitAnswer and full message routing. TypeScript compiles clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build JoinScreen, WaitingScreen, GameHeader, and GameScreen root component</name>

  <files>
    frontend/src/components/JoinScreen.tsx
    frontend/src/components/WaitingScreen.tsx
    frontend/src/components/GameHeader.tsx
    frontend/src/components/GameScreen.tsx
    frontend/src/pages/JoinPage.tsx
  </files>

  <read_first>
    frontend/src/pages/JoinPage.tsx -- current placeholder to replace
    frontend/src/stores/gameStore.ts -- extended store with all Phase 3 fields
    frontend/src/hooks/useWebSocket.ts -- useWebSocket hook for connect/join
    frontend/src/index.css -- existing theme tokens (wb-bg, wb-surface, wb-text, etc.)
    .planning/phases/03-player-frontend/03-UI-SPEC.md -- copywriting contract, spacing scale, typography, color usage rules
    .planning/phases/03-player-frontend/03-CONTEXT.md -- D-12 (screen architecture), D-13 (crossfade transitions), D-14 (header)
    .planning/phases/03-player-frontend/03-RESEARCH.md -- Pattern 3 (AnimatePresence GameScreen)
  </read_first>

  <action>
    Create all five files following the UI-SPEC.md design contract exactly.

    **JoinScreen.tsx** (`frontend/src/components/JoinScreen.tsx`):
    - Export default `function JoinScreen()`.
    - Renders vertically centered layout with max-width 400px card (bg-wb-surface, rounded-xl, p-6).
    - Title "Дуэль чисел" (64px, font-semibold, text-wb-text) centered above the card.
    - Inside card: nickname input (`<input type="text" maxLength={15} placeholder="Введите ваш ник">` styled per UI-SPEC: 16px body font, text-wb-text, bg-wb-bg border, w-full, px-4 py-3, rounded-lg).
    - Below input: "Присоединиться" button. On click: calls `join(nickname)` from useWebSocket hook, sets phase to 'joining'.
    - State: if `phase === 'joining'`, button shows "Отправка..." and input+button are disabled.
    - Use `useGameStore` for phase, `useWebSocket()` for join function.
    - Per UI-SPEC interaction contract: idle phase transitions to joining on click.

    **WaitingScreen.tsx** (`frontend/src/components/WaitingScreen.tsx`):
    - Export default `function WaitingScreen()`.
    - Centered text: if `player2Nickname` is empty (only 1 player joined) show "Ожидание соперника..." (20px, text-wb-text-muted).
    - If `player2Nickname` is set, show "Ожидание запуска администратором" (20px, text-wb-text-muted).
    - Both states include the "Дуэль чисел" title (64px, font-semibold, text-wb-text) centered above.
    - Use `useGameStore` selectors: `phase`, `player1Nickname`, `player2Nickname`.

    **GameHeader.tsx** (`frontend/src/components/GameHeader.tsx`):
    - Export default `function GameHeader()`.
    - Returns a fixed top bar (full-width, bg-wb-surface, py-2 px-4, flex items-center justify-between).
    - Per D-14 and UI-SPEC interaction contract:
      - Left: player's own nickname (16px, font-regular, player accent color). Player accent = `text-player1` if `playerNumber === 1`, `text-player2` if `playerNumber === 2`.
      - Center: "Дуэль чисел" (16px, font-regular, text-wb-text-muted).
      - Right: score (20px, font-semibold, player accent color) + separator + round indicator "Раунд N / 9" (16px, font-regular, text-wb-text-muted).
    - Use `useGameStore` selectors: `playerNumber`, `player1Nickname`/`player2Nickname` (select own nickname based on playerNumber), `player1Score`/`player2Score`, `currentRound`, `totalRounds`.

    **GameScreen.tsx** (`frontend/src/components/GameScreen.tsx`):
    - Export default `function GameScreen()`.
    - Implements Pattern 3 from RESEARCH.md exactly: wraps AnimatePresence `mode="wait"` around phase-based sub-component rendering.
    - Phase routing per D-12 and UI-SPEC state machine:
      - `'idle'` or `'joining'` → `<JoinScreen key="join" />`
      - `'waiting'` → `<WaitingScreen key="waiting" />`
      - `'playing'` → `<PlayingScreen key="playing" />`
      - `'showing_result'` → `<ResultOverlay key="result" />`
      - `'finished'` → `<FinalScreen key="finished" />`
    - Show `<GameHeader />` when `phase` is `'playing'`, `'showing_result'`, or `'finished'` (D-14). Render header before AnimatePresence, not inside it.
    - Root div: `className="relative min-h-screen bg-wb-bg overflow-hidden"`.
    - AnimatePresence wraps only the phase-based content (not the header), so exit animations fire correctly. Per RESEARCH.md anti-pattern: keep AnimatePresence mounted always.
    - Import ResultOverlay and FinalScreen with `@ts-expect-error` or lazy imports since they will be created in the next plan. To be precise: import them normally -- the TypeScript compiler will check, but they won't exist yet. Use forward-declared import with a file that exists as a stub: create minimal placeholder exports. Better approach: `// @ts-ignore` comments on imports for ResultOverlay and FinalScreen, or just define them as inline null-returning stubs at the bottom of GameScreen.tsx.

    **Simplest correct approach:** Define two inline stub components at the bottom of GameScreen.tsx:
    ```
    function ResultOverlay() { return null }
    function FinalScreen() { return null }
    ```
    These will be replaced when 03-02 creates the real components. This avoids TypeScript errors and keeps GameScreen.tsx self-contained.

    **JoinPage.tsx** (`frontend/src/pages/JoinPage.tsx`):
    - Replace entire content with:
    ```
    import GameScreen from '../components/GameScreen'
    export default function JoinPage() {
      return <GameScreen />
    }
    ```

    **Key design rules from UI-SPEC to enforce:**
    - Color: Player 1 accent (#3B82F6) only for Player 1's display elements. Player 2 accent (#EF4444) only for Player 2's display elements. No cross-use per UI-SPEC color contract.
    - Typography: Use the exact size/weight/line-height from UI-SPEC typography table. 16px/400/1.5 for body, 20px/600/1.3 for score/nickname, 36px/600/1.2 for question/answer input (PlayinScreen), 64px/600/1.0 for title/timer.
    - Spacing: Use the 8-point scale (xs=4, sm=8, md=16, lg=24, xl=32, 2xl=48, 3xl=64).
    - All UI text in Russian per UI-SPEC copywriting contract.
    - Input fields: `border-wb-text-muted/30 focus:border-player1` (for JoinScreen input focus).
    - The "Присоединиться" button: min-h-[56px] per D-08 touch target rule. Rounded-lg, bg-player1, text-white, font-semibold, w-full.
    - No external CDN references. All styling via Tailwind classes using `var(--color-*)` theme tokens from index.css.

    **AnimatePresence transition details (D-13):**
    - Each screen wraps its content in `<motion.div>` with:
      - `initial={{ opacity: 0, y: 20 }}`
      - `animate={{ opacity: 1, y: 0 }}`
      - `exit={{ opacity: 0, y: -20 }}`
      - `transition={{ duration: 0.3 }}`
    - Import from `'motion/react'` (not `'framer-motion'`). RESEARCH.md confirms: `import { motion, AnimatePresence } from 'motion/react'`.
  </action>

  <acceptance_criteria>
    - `frontend/src/components/JoinScreen.tsx` exists: contains "Дуэль чисел" title, `<input maxLength={15}>` with "Введите ваш ник" placeholder, "Присоединиться" button, uses useWebSocket().join
    - `frontend/src/components/WaitingScreen.tsx` exists: shows "Ожидание соперника..." or "Ожидание запуска администратором" based on player2Nickname
    - `frontend/src/components/GameHeader.tsx` exists: shows player nickname (left), "Дуэль чисел" (center), score + "Раунд N / 9" (right)
    - `frontend/src/components/GameScreen.tsx` exists: AnimatePresence mode="wait", maps phase to sub-components, header visible during playing/showing_result/finished
    - `frontend/src/pages/JoinPage.tsx` exists: imports and renders `<GameScreen />`
    - GameScreen.tsx has inline stub components for ResultOverlay and FinalScreen (return null)
    - `npx tsc --noEmit` compiles clean from frontend/
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1</automated>
  </verify>

  <done>
    All five files created. JoinScreen, WaitingScreen, GameHeader, GameScreen handle idle/joining/waiting/playing phase rendering using AnimatePresence crossfade transitions. JoinPage delegates to GameScreen. TypeScript compiles clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build TimerRing, AnswerInput, and PlayingScreen</name>

  <files>
    frontend/src/components/TimerRing.tsx
    frontend/src/components/AnswerInput.tsx
    frontend/src/components/PlayingScreen.tsx
  </files>

  <read_first>
    frontend/src/stores/gameStore.ts -- extended store with ws, submittedAnswer, myAnswer
    frontend/src/hooks/useWebSocket.ts -- useWebSocket hook for submitAnswer
    frontend/src/index.css -- theme tokens for colors (player1, player2, warning, danger, correct, etc.)
    .planning/phases/03-player-frontend/03-CONTEXT.md -- D-01/D-02/D-03 (timer), D-08/D-09/D-10/D-11 (answer input), D-12/D-13/D-14 (screens)
    .planning/phases/03-player-frontend/03-UI-SPEC.md -- typography table, color usage rules, interaction contract, spacing scale
    .planning/phases/03-player-frontend/03-RESEARCH.md -- Pattern 2 (TimerRing with Motion pathLength), auto-submit Pitfall 4 mitigation
    .planning/phases/02-core-game-loop/02-CONTEXT.md -- D-12 (first-answer-wins, server-side)
  </read_first>

  <action>
    Create all three files. Follow RESEARCH.md patterns and CONTEXT.md locked decisions exactly.

    **TimerRing.tsx** (`frontend/src/components/TimerRing.tsx`):
    - Follow RESEARCH.md "Pattern 2: Timer Ring with Motion pathLength" exactly.
    - SVG dimensions: 88x88 viewBox, center at (44,44), radius=41, strokeWidth=6. Per UI-SPEC spacing scale exceptions (88px is correct per component geometry).
    - Two `<circle>` elements:
      1. Background ring: stroke="#2d1b4e" (wb-surface), fill="none"
      2. Animated `<motion.circle>`: per D-03 Motion pathLength with `rotate: '-90deg'`, `transformOrigin: 'center'`
    - `pathLength` animated from `remaining / 10` (1.0 at start, 0.0 at expiry). Motion animate prop: `pathLength: progress` with `transition: { duration: 0.3, ease: 'easeOut' }`.
    - Counter-clockwise drain: `pathLength` starts at 1.0 and animates down to 0.0. With `rotate: -90deg`, the circle drains counter-clockwise (per RESEARCH.md Pitfall 3 fix).
    - Color logic per D-02:
      - `remaining <= 3`: `#EF4444` (danger)
      - `remaining <= 5`: `#F59E0B` (warning)
      - Default: player accent -- `#3B82F6` if playerNumber===1, `#EF4444` if playerNumber===2
    - `<text>` element inside SVG: display `remaining` value, centered at (44,44), textAnchor="middle", dominantBaseline="central", fill="#eeeeee" (wb-text), fontFamily="Inter, sans-serif", fontWeight="600", fontSize="64" per D-01 56-72px range (64px chosen per UI-SPEC typography).
    - Add `role="timer"` and `aria-live="polite"` with `aria-label=\`Осталось ${remaining} секунд\`` for accessibility (per UI-SPEC component inventory).
    - Use `useGameStore` selectors: `remaining`, `playerNumber`, `submittedAnswer` (not needed for display, but color needs playerNumber).

    **AnswerInput.tsx** (`frontend/src/components/AnswerInput.tsx`):
    - Per D-08: `<input type="number" min="0" max="1000000">`, full-width (w-full), 36px font (text-3xl font-semibold per typography table), minimum 56px height (min-h-[56px]).
    - Per D-11: Immediate local echo -- typed value updates on keystroke, stored in Zustand `myAnswer` via `setMyAnswer`. Uses controlled component pattern: `value={myAnswer ?? ''}`, `onChange` calls `setMyAnswer(Number(e.target.value))`.
    - Below the input or adjacent: submit button "Ответить" per D-09. Button styling: min-h-[56px], rounded-lg, bg-player1, text-white, font-semibold, px-6.
    - On submit click:
      1. Calls `submitAnswer(myAnswer)` from useWebSocket hook
      2. Sets `submittedAnswer: true` and disables input + button (add `disabled` prop, opacity-50 styling)
      3. Shows "Ответ принят" confirmation text replacing the button text
    - Auto-submit on timer expiry per D-10 with Pitfall 4 mitigation:
      ```
      useEffect(() => {
        if (remaining === 0 && !submittedAnswer && phase === 'playing' && playerNumber) {
          submitAnswer(myAnswer)
          setSubmittedAnswer(true)
        }
      }, [remaining])
      ```
      The guard `!submittedAnswer` prevents double-fire. `phase === 'playing'` prevents stale expiry from previous round. `remaining` is reset to 10 on `round_started` via store's `setRoundStarted`.
    - If input is empty on auto-submit, send `null` (server treats as no answer per D-10).
    - Hint text below input per UI-SPEC copywriting: "Введите целое число от 0 до 1 000 000" (text-wb-text-muted, text-sm).
    - Input disabled styling: when `submittedAnswer` is true, input gets `disabled` attribute and `opacity-50 cursor-not-allowed`.
    - Use `useGameStore` selectors: `myAnswer`, `submittedAnswer`, `remaining`, `phase`, `playerNumber`, `setMyAnswer`, `setSubmittedAnswer`, `ws` (to check connection status).
    - Use `useWebSocket()` hook for `submitAnswer`.

    **PlayingScreen.tsx** (`frontend/src/components/PlayingScreen.tsx`):
    - Export default `function PlayingScreen()`.
    - Renders the active round view per UI-SPEC interaction contract.
    - Three-section layout (flex-col, min-h-screen, px-6):
      1. **Question section** (~40% height via flex-1): centered text of `questionText` from store (36px, font-semibold, text-wb-text, text-center per typography table).
      2. **Input + Timer section** (~30% height via flex-none): relative container.
         - `<AnswerInput />` centered, max-w-md, mx-auto.
         - `<TimerRing />` absolute positioned upper-right of center per UI-SPEC component inventory: `absolute top-0 right-0`.
      3. **Footer/spacer** (~30% height via flex-1): empty, provides bottom space.
    - Use `useGameStore` selectors: `questionText`, `currentRound`, `totalRounds`, `phase`.
    - Import `AnswerInput` and `TimerRing`.
    - The GameScreen wrapper already provides the header (GameHeader). PlayingScreen only renders game content. No header duplication.
    - Wrap content in `<motion.div>` for AnimatePresence transition (initial/animate/exit with fade).

    **Key implementation notes:**
    - All animation imports from `'motion/react'`.
    - No npm install needed -- all deps in package.json.
    - Zero external CDN references. All fonts bundled as WOFF2.
    - The answer input uses `type="number"` which on tablets triggers numeric keyboard. On desktop, browser shows number stepper buttons -- this is acceptable per D-08.
  </action>

  <acceptance_criteria>
    - `frontend/src/components/TimerRing.tsx` exists: 88px SVG circle, radius 41, stroke 6px, pathLength animation, 3 color states (default/warning/danger), 64px bold Inter text, role="timer" with aria-live
    - `frontend/src/components/AnswerInput.tsx` exists: input type=number min=0 max=1000000, 36px font, 56px height, "Ответить" button, auto-submit effect with guard, "Ответ принят" confirmation, hint text
    - `frontend/src/components/PlayingScreen.tsx` exists: three-section layout, question text (36px), AnswerInput + TimerRing, motion.div wrapper
    - TimerRing color: defaults to player1 blue (3B82F6) or player2 red (EF4444) based on playerNumber; warning (F59E0B) at <=5; danger (EF4444) at <=3
    - AnswerInput auto-submit guard checks `remaining === 0 && !submittedAnswer && phase === 'playing'`
    - `npx tsc --noEmit` compiles clean
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1</automated>
  </verify>

  <done>
    TimerRing, AnswerInput, and PlayingScreen created. Timer renders SVG circular countdown with locked color transitions. Answer input supports early submit with "Ответ принят" state and auto-submit on timer expiry. TS compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) -> Server | Untrusted input crosses here (nickname, answer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | WebSocket message handler | mitigate | Client only sends `join` and `submit_answer` events. Server authoritatively validates role assignment (Phase 2 JOIN-02). No client can impersonate another player. |
| T-03-02 | Tampering | AnswerInput auto-submit | mitigate | Auto-submit fires only when `remaining === 0 && !submittedAnswer && phase === 'playing'` triple guard (D-10, Pitfall 4). Server also enforces first-answer-wins per Phase 2 D-12. |
| T-03-03 | Information Disclosure | TimerRing render | mitigate | Timer shows only `remaining` seconds, which is already public via `timer_tick` broadcast. No sensitive data exposed. |
| T-03-04 | Tampering | XSS via nickname | mitigate | React JSX escaping handles injection. Server validates 15-char alphanumeric max (Phase 2). No `dangerouslySetInnerHTML` anywhere in Phase 3 components. |
| T-03-05 | Tampering | Answer input range bypass | accept | Client-side `min=0 max=1000000` is convenience. Server re-validates (Phase 2 D-12) and treats invalid as no answer. No integrity risk. |
| T-03-SC | Tampering | npm install (motion, zustand) | accept | All packages already installed in `package.json` and verified in RESEARCH.md Package Legitimacy Audit. No new installs in Phase 3. |
</threat_model>

<verification>

### Command

```bash
cd frontend && npx tsc --noEmit
```

All TypeScript files must compile without errors.

### Manual Checks

1. Verify all 10 files exist: gameStore.ts, useWebSocket.ts, GameScreen.tsx, JoinScreen.tsx, WaitingScreen.tsx, PlayingScreen.tsx, TimerRing.tsx, AnswerInput.tsx, GameHeader.tsx, JoinPage.tsx
2. Verify JoinScreen has input[maxLength=15], "Присоединиться" button, calls join()
3. Verify WaitingScreen shows correct text per player count
4. Verify GameHeader has nickname (left), title (center), score+round (right)
5. Verify TimerRing has SVG circle, pathLength animation, 3 color states, 64px text
6. Verify AnswerInput has type=number, 56px height, "Ответить" button, auto-submit effect
7. Verify GameScreen uses AnimatePresence mode="wait" with phase-based routing
</verification>

<success_criteria>
1. All 10 files exist and TypeScript compiles clean
2. JoinScreen renders at `/` with nickname input and join button
3. WebSocket hook connects, routes events to store, exposes join/submitAnswer
4. TimerRing renders SVG circular timer with color transitions
5. AnswerInput supports early submit with disabled state and auto-submit on expiry
6. PlayingScreen composes question text, timer, and answer input
7. GameScreen routes between JoinScreen/WaitingScreen/PlayingScreen based on phase
8. GameHeader appears during playing phase with all required info
</success_criteria>

<output>
Create `.planning/phases/03-player-frontend/03-01-SUMMARY.md` when done
</output>
