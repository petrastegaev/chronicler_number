---
quick_id: "260618-ol8"
description: "Импорт вопросов для категории «Числа летописца» из Google Sheets"
created: "2026-06-18"
status: planned
---

# Plan: Импорт вопросов «Числа летописца»

## Цель

Загрузить вопросы из Google Sheets (вкладка gid=732603008) в таблицу `questions` базы данных SQLite через существующий CSV-импорт.

## Исходные данные

Google Sheet: `19d-MiDn1FGICMfiajg5QCqvfhQVblGUv1_Ge-6cAk1I`, вкладка `gid=732603008`
Колонки: `Вопрос`, `Ответ на вопрос`, `Направление` (text, answer, category)

## Задачи

### Задача 1: Очистка и подготовка CSV

**Файлы:** `/tmp/chronicler_questions.csv`

**Действие:**
1. Скачать Google Sheet как CSV (уже сделано — файл `/tmp/chronicler_questions.csv`)
2. Исправить нецелочисленные ответы:
   - `"2048 или 2 KB"` → `2048` (размер стека горутины в байтах)
   - `"65 535"` → `65535` (количество портов TCP/IP)
   - `"86 400"` → `86400` (секунд в сутках)
   - `"1 000"` → `1000` (миллисекунд в секунде)
   - `"4 294 967 296"` → `4294967296` (количество IPv4-адресов)
   - `"10 (owasp top 10)"` → `10` (позиций в OWASP TOP 10)
   - `"2 (gpg & ssh)"` → `2` (видов подписи Git)
   - `"8.5"` → `8` (RPS в миллионах — округление до целого)
3. Сохранить очищенный CSV в `data/chronicler_questions.csv`

**Проверка:** Все строки CSV имеют целочисленный ответ в диапазоне 0-1 000 000.

### Задача 2: Импорт вопросов в БД

**Файлы:** `data/chronicler_questions.csv`, `backend/services/question_service.py`

**Действие:** Импортировать CSV через существующий API endpoint `POST /api/questions/upload-csv` или напрямую через `QuestionService.csv_import()`.

**Проверка:** 
- Все вопросы из CSV присутствуют в таблице `questions`
- Категории корректно проставлены (AI и ML, Go, Общие, ИБ, ML, WB, Russ)
- Количество импортированных вопросов: ~106 (после фильтрации пустых строк)
