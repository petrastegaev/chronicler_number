services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8081:8000"
    volumes:
      - data:/app/data
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

volumes:
  data:
