---
phase: 04-admin-panel-question-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/routers/questions.py
  - frontend/src/stores/adminStore.ts
  - frontend/src/components/admin/QuestionsTab.tsx
  - frontend/src/components/admin/QuestionListTab.tsx
  - frontend/src/components/admin/QuestionAddTab.tsx
  - frontend/src/components/admin/ConfirmDialog.tsx
  - frontend/src/components/admin/Toast.tsx
  - frontend/src/pages/AdminPage.tsx
autonomous: true
requirements:
  - QUEST-01
  - QUEST-02
  - QUEST-03

user_setup: []

must_haves:
  truths:
    - "Admin can view all questions in a paginated table on the Список sub-tab"
    - "Question count badge appears on the Список sub-tab label (e.g. Список (42))"
    - "Admin can add a single question via form with text, answer, and optional category"
    - "Admin can delete a question with confirmation dialog and toast notification"
    - "Empty state shows guidance text when no questions exist"
  artifacts:
    - path: "frontend/src/components/admin/QuestionsTab.tsx"
      provides: "Tab shell with sub-tab bar for Список, Добавить, CSV"
      exists: true
    - path: "frontend/src/components/admin/QuestionListTab.tsx"
      provides: "Paginated question list with delete per row"
      exists: true
    - path: "frontend/src/components/admin/QuestionAddTab.tsx"
      provides: "Single question add form"
      exists: true
    - path: "frontend/src/components/admin/ConfirmDialog.tsx"
      provides: "Reusable confirmation dialog with AnimatePresence"
      exists: true
    - path: "frontend/src/components/admin/Toast.tsx"
      provides: "Auto-dismiss toast notification"
      exists: true
    - path: "backend/routers/questions.py"
      provides: "Question list now includes total count in response"
      contains: "total"
  key_links:
    - from: "QuestionsTab.tsx"
      to: "QuestionListTab / QuestionAddTab"
      via: "conditional render by active sub-tab"
      pattern: "mode=\"wait\""
    - from: "QuestionListTab.tsx"
      to: "ConfirmDialog"
      via: "import + render on delete tap"
      pattern: "ConfirmDialog"
    - from: "QuestionAddTab.tsx"
      to: "adminStore.setQuestions"
      via: "refreshQuestionList after add"
      pattern: "setQuestions"
---

<objective>
Build the question management frontend: sub-tab navigation (Список | Добавить | CSV placeholder), paginated question list with delete, single-question add form, confirmation dialog, and toast notifications.

Purpose: Admin can view, add, and delete questions. The question list is cached and only refreshed after mutations per D-06. The CSV sub-tab shows a placeholder that will be replaced in Plan 04-03.

Output:
- `backend/routers/questions.py` — modified GET / to include total count in response
- `frontend/src/components/admin/QuestionsTab.tsx` — sub-tab bar + content switching
- `frontend/src/components/admin/QuestionListTab.tsx` — paginated question table
- `frontend/src/components/admin/QuestionAddTab.tsx` — single question form
- `frontend/src/components/admin/ConfirmDialog.tsx` — reusable confirmation modal
- `frontend/src/components/admin/Toast.tsx` — auto-dismiss toast
- `frontend/src/pages/AdminPage.tsx` — replaces placeholder questions tab
- `frontend/src/stores/adminStore.ts` — extends with question management state (questions, totalQuestions, csvResult)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-panel-question-management/04-CONTEXT.md
@.planning/phases/04-admin-panel-question-management/04-RESEARCH.md
@.planning/phases/04-admin-panel-question-management/04-UI-SPEC.md

# Existing patterns to follow (read before modifying)
@frontend/src/stores/adminStore.ts (created in 04-01, extend with question fields)
@frontend/src/pages/AdminPage.tsx (created in 04-01, add QuestionsTab render)
@frontend/src/types/ws.ts (existing event types)
@../../components/ResultOverlay.tsx (motion AnimatePresence pattern)

# Created in this task
@frontend/src/components/admin/QuestionsTab.tsx
@frontend/src/components/admin/QuestionListTab.tsx
@frontend/src/components/admin/QuestionAddTab.tsx
@frontend/src/components/admin/ConfirmDialog.tsx
@frontend/src/components/admin/Toast.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add total count to question list response</name>
  <files>
    backend/routers/questions.py
  </files>
  <read_first>
    backend/routers/questions.py (existing GET / endpoint)
    backend/services/question_service.py (existing get_all that returns list)
  </read_first>
  <action>
    The current `GET /api/questions/` returns `list[QuestionResponse]`. The frontend needs to know the total question count for pagination and the badge display per D-03.

    Modify the GET "/" endpoint in `backend/routers/questions.py`:
    - Add a `count` query: `await db.execute(select(func.count(Question.id)))` to get total count
    - Import `func` from sqlalchemy and `Question` from models
    - Return a dict `{"items": [...questions], "total": count}` instead of a bare list
    - The response model changes from `list[QuestionResponse]` to a dict — remove the response_model annotation on the endpoint function (let FastAPI infer from the dict return)
    - The `items` key contains `list[QuestionResponse]` serialized questions
    - The `total` key contains the integer count

    Implementation notes:
    - Keep `skip` and `limit` query params unchanged
    - `count` query uses `select(func.count(Question.id))` — scalar result
    - Return `{"items": questions, "total": total_count}` where `questions` is the list from `QuestionService.get_all()`
  </action>
  <acceptance_criteria>
    - `GET /api/questions/?skip=0&limit=20` returns `{"items": [...], "total": N}` not a bare array
    - The returned questions are still serialized as QuestionResponse
    - Backward compatible for the existing skip/limit params
  </acceptance_criteria>
  <verify>
    <automated>cd /home/petr/IdeaProjects/PetProjects/number_game/backend && python -c "from routers import questions; print('router imported')"</automated>
  </verify>
  <done>Question list endpoint returns items+total format. Python imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — ConfirmDialog, Toast, adminStore question fields</name>
  <files>
    frontend/src/components/admin/ConfirmDialog.tsx
    frontend/src/components/admin/Toast.tsx
    frontend/src/stores/adminStore.ts
  </files>
  <read_first>
    frontend/src/stores/adminStore.ts (existing store from 04-01 — add question fields and actions)
    frontend/src/components/ResultOverlay.tsx (Motion AnimatePresence pattern reference)
  </read_first>
  <action>
    Extend `frontend/src/stores/adminStore.ts`:
    - The store already has `questions`, `totalQuestions`, and `csvResult` fields from the initial interface in 04-01, but ensure the setter actions are defined:
      - `setQuestions(questions: Question[])` — replaces the question list array
      - `setTotalQuestions(n: number)` — sets total count
      - `setCsvResult(result: { added: number; errors: string[] } | null)` — stores CSV import result
      - `clearCsvResult()` — sets csvResult to null (when re-entering CSV tab)
    - Import or define a `Question` type inline: `{ id: number; text: string; answer: number; category: string | null; created_at: string }`

    Create `frontend/src/components/admin/Toast.tsx`:
    - Import `motion`, `AnimatePresence` from 'motion/react'
    - Import useState, useEffect from react
    - Props: `message: string | null` (null = hidden)
    - Fixed container: `fixed top-4 left-4 right-4 z-50 flex justify-center pointer-events-none`
    - Card: bg `wb-surface`, rounded-lg, px-5 py-3, shadow-lg, border `wb-text-muted/20`
    - Text: 14px semibold, centered
    - Uses AnimatePresence: when message is non-null, show motion.div with fade+slide from top (-20px y) animation, 0.25s duration
    - Auto-dismiss: when message changes from null to a value, start a setTimeout(3000ms) that calls `onDismiss` prop
    - Props: `message: string | null`, `onDismiss: () => void`
    - Export default

    Create `frontend/src/components/admin/ConfirmDialog.tsx`:
    - Import { motion, AnimatePresence } from 'motion/react'
    - Props: `isOpen: boolean`, `title: string`, `body: string`, `confirmLabel: string`, `cancelLabel: string`, `onConfirm: () => void`, `onCancel: () => void`, `confirmVariant?: 'danger' | 'primary'` (default 'danger')
    - Render only when isOpen is true (use AnimatePresence)
    - Full-screen fixed overlay: `fixed inset-0 z-50 bg-[rgba(26,10,46,0.85)]`
    - Centered card: bg `wb-surface`, rounded-xl, p-6, max-w-sm, mx-4
    - Title: 18px semibold, wb-text
    - Body: 14px, wb-text-muted, mt-2
    - Two buttons side by side: flex-row gap-3 mt-6
      - Cancel: outline style (border wb-text-muted/30, flex-1, min-h-[44px], rounded-lg)
      - Confirm: bg `danger` if confirmVariant='danger', bg `player1` if 'primary', flex-1, min-h-[44px], rounded-lg
    - Animation: overlay fade 0.2s, card scale from 0.9 + fade 0.2s
    - Export default
  </action>
  <acceptance_criteria>
    - adminStore has setQuestions, setTotalQuestions, setCsvResult, clearCsvResult actions
    - Toast renders centered fixed top with fade+slide animation, auto-dismisses after 3s
    - ConfirmDialog shows overlay + centered card with title, body, cancel/confirm buttons
    - `npx tsc --noEmit` compiles clean
  </acceptance_criteria>
  <verify>
    <automated>cd /home/petr/IdeaProjects/PetProjects/number_game/frontend && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Toast, ConfirmDialog, and adminStore question fields compile clean. Components follow existing motion/AnimatePresence patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — QuestionsTab with sub-tabs, QuestionListTab, QuestionAddTab, wire into AdminPage</name>
  <files>
    frontend/src/components/admin/QuestionsTab.tsx
    frontend/src/components/admin/QuestionListTab.tsx
    frontend/src/components/admin/QuestionAddTab.tsx
    frontend/src/pages/AdminPage.tsx
  </files>
  <read_first>
    frontend/src/pages/AdminPage.tsx (existing from 04-01 — add QuestionsTab render)
    adminStore.ts (for state selectors)
    ConfirmDialog.tsx (created in Task 2)
    Toast.tsx (created in Task 2)
    ../../index.css (color tokens)
  </read_first>
  <action>
    Create `frontend/src/components/admin/QuestionsTab.tsx`:
    - Import useState from react
    - Import QuestionListTab, QuestionAddTab components
    - Import AnimatePresence, motion from 'motion/react'
    - State: `activeSubTab: 'list' | 'add' | 'csv'` — default 'list' per D-02
    - Sub-tab bar: horizontal flex row, 44px height, bg-transparent
      - "Список (N)" — N from adminStore.totalQuestions (select with `useAdminStore(s => s.totalQuestions)`)
      - "Добавить"
      - "CSV"
    - Active sub-tab: bottom border 2px `player1` color, text wb-text
    - Inactive: no border, text wb-text-muted
    - Each sub-tab: px-4, min-h-[44px], flex items-center, cursor-pointer, text-sm font-semibold
    - Content area: AnimatePresence mode="wait"
      - list → `<QuestionListTab />`
      - add → `<QuestionAddTab />`
      - csv → placeholder div with "CSV импорт (будет реализовано)" text (14px muted, centered, mt-8)
    - Wrap content with motion.div for crossfade animation (opacity only per UI-SPEC sub-tab animation contract)

    Create `frontend/src/components/admin/QuestionListTab.tsx`:
    - Import useState, useEffect, useCallback from react
    - Import motion, AnimatePresence from 'motion/react'
    - Import useAdminStore
    - Import ConfirmDialog, Toast
    - State: `page` (number, 0-based), `loading` (boolean), `error` (string|null), `deleteTarget` (Question|null), `toast` (string|null)
    - On mount and on page change: fetch `GET /api/questions?skip={page*20}&limit=20`
      - Response is `{items: Question[], total: number}`
      - Call `useAdminStore.getState().setQuestions(items)` and `setTotalQuestions(total)`
    - Loading state: centered spinner text "Загрузка..." (14px muted)
    - Error state: "Ошибка загрузки" in danger color
    - Empty state (totalQuestions === 0): centered "Нет вопросов" (18px semibold) + body text "Добавьте вопросы через форму «Добавить» или импортируйте через CSV." (14px muted)
    - Row list: each row is a motion.div with layout prop
      - Row structure: ID (#N 14px muted mono, min-w for 3 digits), question text (14px semibold, flex-1, truncate), category pill (14px muted, bg-wb-bg, rounded, px-2, hidden if null), delete button (trash SVG 20x20, danger color, p-3 for 44px touch target, aria-label="Удалить вопрос")
      - Row padding: py-3 px-4
      - Alternating bg: even bg-wb-surface, odd bg-wb-surface/50
    - Bottom: if has more pages (`total > (page+1)*20`), "Показать ещё" button (14px player1, text-center, py-4, cursor-pointer)
      - If no more pages and page > 0: "Конец списка" (14px muted, text-center, py-4)
    - On delete button tap: set `deleteTarget` to the question
    - ConfirmDialog: shown when deleteTarget is set
      - title: "Удалить вопрос?"
      - body: "Вопрос «{truncated text (max 60 chars)}» будет удалён без возможности восстановления."
      - confirmLabel: "Удалить" (danger variant)
      - cancelLabel: "Отмена"
      - onConfirm: `DELETE /api/questions/{id}` → if 204, remove from local list (filter out), decrement totalQuestions, show toast "Вопрос удалён"
      - onCancel: clear deleteTarget
    - Toast at component level: shown when toast state is non-null, onDismiss clears it

    Create `frontend/src/components/admin/QuestionAddTab.tsx`:
    - Import useState, useCallback from react
    - Import motion from 'motion/react'
    - Import useAdminStore
    - Import Toast
    - Form inside card: bg wb-surface, rounded-xl, p-4
    - Fields:
      - "Текст вопроса" input: type="text", required, maxLength=500, placeholder="Введите вопрос"
      - "Ответ" input: type="number", required, min=0, max=1000000, placeholder="Введите ответ"
      - "Категория (необязательно)" input: type="text", optional, maxLength=255, placeholder="Категория (необязательно)"
      - All inputs: bg wb-bg, border wb-text-muted/30, rounded-lg, px-4 py-3, text-base, w-full, mt-3, focus:border-player1 focus:outline-none
      - Labels above each input: 14px semibold wb-text-muted, first one has no mt (top of card)
    - "Добавить вопрос" button: full-width, bg-player1, min-h-[44px], rounded-lg, mt-4, font-semibold
      - Disabled (opacity-50) unless text AND answer are non-empty
      - On click: POST /api/questions/ with JSON body {text, answer, category}
      - On success (201): show toast "Вопрос добавлен", clear form fields, refresh question list via `GET /api/questions?skip=0&limit=20` + setQuestions/setTotalQuestions
      - On error: show error text below button in 14px danger color: "Ошибка при добавлении вопроса: {server_error}" or "Ошибка сети. Проверьте соединение."
    - Error text: state variable `formError: string | null`, shown as 14px danger text below button when non-null
    - Toast at component level

    Modify `frontend/src/pages/AdminPage.tsx`:
    - Import QuestionsTab from components/admin/QuestionsTab
    - In the questions tab render branch (where placeholder was), render `<QuestionsTab />` instead
  </action>
  <acceptance_criteria>
    - QuestionsTab shows sub-tab bar with Список, Добавить, CSV (CSV shows placeholder)
    - QuestionListTab renders paginated list of questions fetched from API
    - Empty state shows correct Russian text guidance
    - Delete shows confirmation dialog with truncated question text
    - Delete removes the row with animation and shows toast
    - QuestionAddTab form validates both fields, submits to API, refreshes list on success
    - Server errors shown below submit button
    - `npx tsc --noEmit` compiles clean
  </acceptance_criteria>
  <verify>
    <automated>cd /home/petr/IdeaProjects/PetProjects/number_game/frontend && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Question management works end-to-end: list with pagination, add single question, delete with confirmation. All TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> Question REST API | HTTP calls for CRUD operations on questions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | DELETE /api/questions/{id} | accept | Any client can delete questions. Acceptable for local WiFi booth with no auth. ASVS V2, V5 not applicable. |
| T-04-04 | Spoofing | POST /api/questions | accept | Same rationale — no auth on local-only deployment. |
| T-04-SC | Tampering | npm/pip installs | mitigate | No new packages introduced in this plan. |
</threat_model>

<verification>
Run `npx tsc --noEmit` from frontend/ — zero errors.
Run Python import check from backend/.
Verify question list returns items+total format.
</verification>

<success_criteria>
1. Question list sub-tab shows paginated list with question count badge
2. Admin can add a single question via form on Добавить sub-tab
3. Admin can delete a question with confirmation dialog and toast
4. Admin can switch between sub-tabs with smooth crossfade animation
5. Empty state displays guidance text when no questions exist
</success_criteria>

<output>
Create `.planning/phases/04-admin-panel-question-management/04-02-SUMMARY.md` when done
</output>
